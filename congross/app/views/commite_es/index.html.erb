<p id="notice"><%= notice %></p>

<h1>Commitees</h1>

<table>
  <thead>
    <tr>
      <th>Propublica</th>
      <th>Name</th>
      <th>Chamber</th>
      <th>Url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @commitees.each do |commitee| %>
      <tr>
        <td><%= commitee.propublica_id %></td>
        <td><%= commitee.name %></td>
        <td><%= commitee.chamber %></td>
        <td><%= commitee.url %></td>
        <td><%= link_to 'Show', commitee %></td>
        <td><%= link_to 'Edit', edit_commitee_path(commitee) %></td>
        <td><%= link_to 'Destroy', commitee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Commitee', new_commitee_path %>
